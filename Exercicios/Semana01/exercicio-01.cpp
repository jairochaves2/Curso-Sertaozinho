#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[]) {
  float x,n,res;
  x=11;
  printf("digite o numero:");
  scanf("%f", &n );
  res=x/n;
  printf("o numero eh: %f", res);
  return 0;
}
